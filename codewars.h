#include <stdlib.h>

void two_oldest_ages(size_t, const int[], int[]);

int sum_digits(int);

void rowWeights(const int[], int, int[]);

cmpFunc(const void *, const void *);

minValues(int *, const size_t);